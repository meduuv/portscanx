# Contributing

Use Python 3.10 or newer. Keep the project dependency-free unless a dependency has a clear benefit.

Run the test suite with:

```text
python -m unittest discover -s tests
```

Keep scanning behavior limited to authorized diagnostics and testing environments.
